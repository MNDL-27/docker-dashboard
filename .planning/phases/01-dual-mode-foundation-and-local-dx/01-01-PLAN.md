---
phase: 01-dual-mode-foundation-and-local-dx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - docker-compose.base.yml
  - docker-compose.dev.yml
  - scripts/dev-saas-local.mjs
  - scripts/seed-saas-local.mjs
  - prisma/seed-local.ts
  - .env.saas-local.example
autonomous: true
requirements:
  - COMP-01
  - DEVX-01
  - DEVX-02
must_haves:
  truths:
    - "Developer can run one command to boot db, redis, localstack, cloud-api, cloud-web, and local agent."
    - "Local SaaS startup waits for readiness instead of reporting partial boot states."
    - "Seeded local data and enrollment token exist immediately after bootstrap."
    - "Self-hosted compose lane remains runnable alongside SaaS-local lane."
  artifacts:
    - path: "scripts/dev-saas-local.mjs"
      provides: "Single orchestration command for SaaS-local stack startup and readiness checks."
      contains: "docker compose -f docker-compose.base.yml -f docker-compose.dev.yml up --build --wait"
    - path: "scripts/seed-saas-local.mjs"
      provides: "Deterministic schema apply plus seed flow for local SaaS mode."
      contains: "prisma db push"
    - path: "prisma/seed-local.ts"
      provides: "Local seed records for first user/org/project and initial host enrollment token."
      contains: "dev-local-agent-token"
    - path: "docker-compose.dev.yml"
      provides: "Cloud API, cloud web, and agent services wired to seeded local defaults."
      contains: "AGENT_TOKEN"
  key_links:
    - from: "package.json"
      to: "scripts/dev-saas-local.mjs"
      via: "npm run dev:saas-local"
      pattern: "dev:saas-local"
    - from: "scripts/dev-saas-local.mjs"
      to: "scripts/seed-saas-local.mjs"
      via: "post-start bootstrap call"
      pattern: "seed-saas-local"
    - from: "prisma/seed-local.ts"
      to: "docker-compose.dev.yml"
      via: "shared AGENT_TOKEN value"
      pattern: "dev-local-agent-token"
---

<objective>
Establish a deterministic SaaS-local bootstrap lane with one documented command while keeping self-hosted runtime continuity intact.

Purpose: Satisfy local developer setup requirements without destabilizing current self-hosted users.
Output: New bootstrap scripts, compose wiring, and seed artifacts used by a single root command.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dual-mode-foundation-and-local-dx/CONTEXT.md
@.planning/phases/01-dual-mode-foundation-and-local-dx/01-RESEARCH.md
@package.json
@docker-compose.base.yml
@docker-compose.dev.yml
@docker-compose.example.yml
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add one-command SaaS-local bootstrap entrypoint</name>
  <files>package.json, scripts/dev-saas-local.mjs, .env.saas-local.example</files>
  <action>Create `scripts/dev-saas-local.mjs` and wire `npm run dev:saas-local` in root `package.json`. The script must run `docker compose config` for `docker-compose.base.yml` + `docker-compose.dev.yml`, then execute `docker compose ... up --build --wait`. Include clear fail-fast output if required env values are missing and print post-start verification URLs. Keep this as the canonical one-command entrypoint (use npm per user-flexible choice; do not introduce make-only flow).</action>
  <verify>Run `npm run dev:saas-local` and confirm command exits with success once services are healthy; run `docker compose -f docker-compose.base.yml -f docker-compose.dev.yml ps` and confirm db, redis, localstack, api, web, and agent are running/healthy.</verify>
  <done>One root command boots the full SaaS-local runtime deterministically and exposes clear success/failure state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic schema + seed bootstrap for local mode</name>
  <files>scripts/seed-saas-local.mjs, prisma/seed-local.ts, package.json</files>
  <action>Add a local bootstrap script that applies schema (`prisma db push` for local mode), then runs a deterministic seed (`prisma/seed-local.ts`) creating baseline user/org/project records and an enrollment token consumed by local agent startup. Ensure idempotent seed behavior (re-runs do not duplicate baseline entities). Keep this local-only flow and avoid introducing tenancy/auth feature expansion beyond seeded baseline records.</action>
  <verify>Run `node scripts/seed-saas-local.mjs`; then run `docker compose -f docker-compose.base.yml -f docker-compose.dev.yml exec -T db psql -U app -d docker_dashboard -c "select count(*) from \"User\";"` and confirm count >= 1; check agent logs with `docker compose -f docker-compose.base.yml -f docker-compose.dev.yml logs agent` and confirm no missing-token fatal.</verify>
  <done>Schema is applied and baseline local records exist after bootstrap; local agent can start using seeded enrollment token path.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve explicit self-hosted lane compatibility boundary</name>
  <files>package.json, scripts/dev-saas-local.mjs</files>
  <action>Add non-destructive mode boundary checks so SaaS-local bootstrap does not modify `docker-compose.example.yml` or self-hosted defaults. Add a root script alias for self-hosted compose bring-up/down (`mode:selfhosted:up`/`mode:selfhosted:down`) to keep legacy runtime first-class. Do not rewrite legacy server/public runtime behavior.</action>
  <verify>Run `npm run mode:selfhosted:up` and confirm `docker compose -f docker-compose.example.yml config` succeeds; run `npm run mode:selfhosted:down` to cleanly stop lane.</verify>
  <done>Self-hosted lane remains explicitly runnable and isolated from SaaS-local bootstrap changes.</done>
</task>

</tasks>

<verification>
Execute both runtime lanes independently: self-hosted lane via `docker-compose.example.yml` and SaaS-local lane via `npm run dev:saas-local`. Confirm neither lane requires editing the other's files to boot.
</verification>

<success_criteria>
One documented command boots local cloud-api, cloud-web, data services, and local agent with seeded baseline setup, while legacy self-hosted startup remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dual-mode-foundation-and-local-dx/01-01-SUMMARY.md`
</output>
