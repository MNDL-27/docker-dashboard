---
phase: 01-dual-mode-foundation-and-local-dx
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/config/transport.ts
  - apps/api/src/index.ts
  - apps/api/src/routes/hosts.ts
  - apps/web/src/lib/transport.ts
  - apps/web/src/lib/api.ts
  - apps/web/next.config.ts
  - packages/agent/config/transport.go
  - packages/agent/main.go
  - packages/agent/client/websocket.go
autonomous: true
requirements:
  - SECU-02
must_haves:
  truths:
    - "Production startup fails fast when HTTP or WS URLs are configured for cloud API or agent paths."
    - "Local development still allows HTTP/WS defaults for frictionless local workflows."
    - "Agent websocket path resolves to WSS when production base URL is HTTPS."
  artifacts:
    - path: "apps/api/src/config/transport.ts"
      provides: "Central URL/protocol validator for API runtime configuration."
      contains: "assertProdTransport"
    - path: "apps/web/src/lib/transport.ts"
      provides: "Web-side production transport guard for NEXT_PUBLIC_API_URL and derived websocket URLs."
      contains: "https:"
    - path: "packages/agent/config/transport.go"
      provides: "Agent production transport validation and allowed-scheme enforcement."
      contains: "wss"
  key_links:
    - from: "apps/api/src/index.ts"
      to: "apps/api/src/config/transport.ts"
      via: "startup config validation"
      pattern: "assertProdTransport"
    - from: "apps/web/next.config.ts"
      to: "apps/web/src/lib/transport.ts"
      via: "rewrite destination guard"
      pattern: "NEXT_PUBLIC_API_URL"
    - from: "packages/agent/main.go"
      to: "packages/agent/config/transport.go"
      via: "startup validation before enrollment/ws connect"
      pattern: "ValidateProdTransport"
---

<objective>
Enforce HTTPS/WSS-only production transport paths across API, web, and agent startup flows.

Purpose: Prevent insecure production deployments from booting and reduce mixed-content or plaintext transport risk.
Output: Centralized transport validation utilities and startup guard wiring in all production-facing runtimes.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dual-mode-foundation-and-local-dx/CONTEXT.md
@.planning/phases/01-dual-mode-foundation-and-local-dx/01-RESEARCH.md
@apps/api/src/index.ts
@apps/web/src/lib/api.ts
@apps/web/next.config.ts
@packages/agent/main.go
@packages/agent/client/websocket.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API production transport guardrails</name>
  <files>apps/api/src/config/transport.ts, apps/api/src/index.ts, apps/api/src/routes/hosts.ts</files>
  <action>Create a centralized API transport validator and call it at API startup before server listen. In production mode, enforce `https:` for public API and CORS URL inputs and enforce `wss:` for any explicit websocket endpoint configs; reject invalid or insecure values with actionable errors. Keep development defaults (`http://localhost`) working. Update host enrollment/install snippet generation to avoid emitting insecure production defaults.</action>
  <verify>Run `npm --prefix apps/api run build`; run `NODE_ENV=production CORS_ORIGIN=http://example.com node apps/api/dist/index.js` and confirm startup exits with transport validation error; run `NODE_ENV=development npm --prefix apps/api run dev` and confirm API still boots locally.</verify>
  <done>API refuses insecure production URL schemes and still supports current local development transport defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Add web and agent production transport guardrails</name>
  <files>apps/web/src/lib/transport.ts, apps/web/src/lib/api.ts, apps/web/next.config.ts, packages/agent/config/transport.go, packages/agent/main.go, packages/agent/client/websocket.go</files>
  <action>Add transport validation in web and agent startup paths. Web must enforce `NEXT_PUBLIC_API_URL` as `https:` in production and ensure derived websocket endpoints use `wss:`. Agent must validate API URL scheme in production mode and fail before enrollment when insecure values are supplied. Preserve local development behavior (`http://` and derived `ws://`) outside production.</action>
  <verify>Run `npm --prefix apps/web run build` with `NEXT_PUBLIC_API_URL=http://localhost:3001 NODE_ENV=production` and confirm guarded failure; run `NEXT_PUBLIC_API_URL=https://api.example.com NODE_ENV=production npm --prefix apps/web run build` and confirm success. Run `cd packages/agent && go test ./...` plus `AGENT_ENV=production AGENT_API_URL=http://api.example.com go run .` and confirm fail-fast error.</verify>
  <done>Web and agent enforce secure transport in production and continue to run with current local defaults in development.</done>
</task>

</tasks>

<verification>
Execute explicit insecure-production smoke checks for API, web, and agent to confirm each runtime fails fast with clear transport error messages.
</verification>

<success_criteria>
Production configuration paths reject HTTP/WS and only accept HTTPS/WSS for UI/API/agent communication contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dual-mode-foundation-and-local-dx/01-02-SUMMARY.md`
</output>
