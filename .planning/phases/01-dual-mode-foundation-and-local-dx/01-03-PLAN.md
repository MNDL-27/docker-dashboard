---
phase: 01-dual-mode-foundation-and-local-dx
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - README.md
  - docs/self-hosted-mode.md
  - docs/saas-local-mode.md
  - docs/mode-verification.md
autonomous: true
requirements:
  - COMP-02
  - DEVX-02
  - COMP-01
must_haves:
  truths:
    - "Users can identify which mode to run (self-hosted vs SaaS-local) without ambiguity."
    - "Users can follow mode-specific commands and environment setup from root docs."
    - "Users can verify each mode independently with explicit health checks and expected outputs."
    - "Self-hosted workflow remains documented as first-class while cloud-local workflow is clearly separated."
  artifacts:
    - path: "README.md"
      provides: "Top-level mode chooser and links to dedicated run-mode guides."
      contains: "Self-hosted mode"
    - path: "docs/self-hosted-mode.md"
      provides: "Legacy mode quickstart, env variables, and verification commands."
      contains: "docker-compose.example.yml"
    - path: "docs/saas-local-mode.md"
      provides: "One-command SaaS-local startup, seed flow, and troubleshooting."
      contains: "npm run dev:saas-local"
    - path: "docs/mode-verification.md"
      provides: "Post-setup acceptance checklist for both lanes and transport guard behavior."
      contains: "HTTPS/WSS"
  key_links:
    - from: "README.md"
      to: "docs/self-hosted-mode.md"
      via: "mode chooser links"
      pattern: "self-hosted-mode"
    - from: "README.md"
      to: "docs/saas-local-mode.md"
      via: "mode chooser links"
      pattern: "saas-local-mode"
    - from: "docs/saas-local-mode.md"
      to: "scripts/dev-saas-local.mjs"
      via: "documented one-command entrypoint"
      pattern: "dev:saas-local"
---

<objective>
Publish clear, mode-separated documentation so users and developers can run self-hosted and SaaS-local lanes confidently.

Purpose: Remove workflow ambiguity and make both supported run modes verifiable from docs alone.
Output: Updated root README plus dedicated mode guides and verification checklist.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dual-mode-foundation-and-local-dx/CONTEXT.md
@.planning/phases/01-dual-mode-foundation-and-local-dx/01-RESEARCH.md
@README.md
@docs/README.md
@docker-compose.example.yml
@docker-compose.base.yml
@docker-compose.dev.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split root docs into explicit run-mode lanes</name>
  <files>README.md, docs/self-hosted-mode.md, docs/saas-local-mode.md</files>
  <action>Refactor root README into a concise mode selector with two clear paths: Self-hosted mode and SaaS-local mode. Create dedicated docs for each lane with separate prerequisites, env vars, startup/shutdown commands, and expected URLs. Keep language explicit that both lanes are supported in Phase 1 and avoid mixing command sets on a single flow.</action>
  <verify>Open `README.md` and confirm both mode links are present; follow each doc top-to-bottom and ensure command sequence is mode-pure (no cross-mode command leakage).</verify>
  <done>Documentation presents unambiguous mode-specific workflows and preserves self-hosted as first-class.</done>
</task>

<task type="auto">
  <name>Task 2: Document seeded local setup and secure-transport expectations</name>
  <files>docs/saas-local-mode.md, docs/mode-verification.md</files>
  <action>Document the exact one-command local startup (`npm run dev:saas-local`), required environment setup, seeded baseline user/org/project behavior, and local agent token bootstrap expectations. Add verification checklist steps proving production transport guardrails reject HTTP/WS while local mode allows HTTP/WS. Include troubleshooting for seed/idempotency and service readiness errors.</action>
  <verify>Run every command in `docs/mode-verification.md` and confirm expected pass/fail outcomes are clear and reproducible on a clean workspace.</verify>
  <done>Developer can reproduce local stack + seed setup and understand secure transport behavior from docs alone.</done>
</task>

</tasks>

<verification>
Perform a docs-only dry run: a contributor who has not seen implementation details should be able to start either mode and verify expected behavior by following the written steps exactly.
</verification>

<success_criteria>
Docs clearly separate self-hosted versus SaaS-local workflows, include one-command local bootstrap plus seeded setup, and describe production HTTPS/WSS guard expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dual-mode-foundation-and-local-dx/01-03-SUMMARY.md`
</output>
