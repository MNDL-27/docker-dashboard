---
phase: 02-identity-access-and-tenant-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/config/session.ts
  - apps/api/src/index.ts
  - apps/api/src/routes/auth.ts
  - apps/api/src/middleware/auth.ts
  - apps/api/src/lib/validation/auth.ts
  - apps/web/src/lib/api.ts
  - apps/web/src/app/login/page.tsx
  - apps/web/src/app/register/page.tsx
  - apps/web/src/app/page.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - User can create an account with email and password and immediately becomes authenticated.
    - User can log in with valid credentials and invalid credentials fail without leaking account details.
    - User remains authenticated across refreshes until session expiry and can fetch current user identity.
    - User can log out and the prior session no longer grants access to protected endpoints.
  artifacts:
    - path: apps/api/src/routes/auth.ts
      provides: Register, login, me, and logout endpoints with secure session lifecycle.
      exports: [default]
    - path: apps/api/src/config/session.ts
      provides: PostgreSQL-backed session configuration with secure cookie settings.
      contains: sessionMiddleware
    - path: apps/web/src/app/login/page.tsx
      provides: Login form and error handling tied to cookie-auth API flow.
    - path: apps/web/src/app/register/page.tsx
      provides: Registration form and post-register authenticated redirect.
  key_links:
    - from: apps/api/src/routes/auth.ts
      to: apps/api/src/config/session.ts
      via: req.session regenerate/save/destroy usage in register/login/logout.
      pattern: req\.session\.(regenerate|save|destroy)
    - from: apps/web/src/lib/api.ts
      to: /auth/login
      via: fetch credentials include for cookie-based auth.
      pattern: credentials:\s*'include'
---

<objective>
Deliver robust account and session primitives so authentication behavior is secure, predictable, and reusable by all tenant-scoped routes.

Purpose: Establish a stable auth baseline before layering organization/project authorization logic.
Output: Hardened API auth/session flow and aligned web login/register/logout behavior.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-access-and-tenant-isolation/CONTEXT.md
@.planning/phases/02-identity-access-and-tenant-isolation/02-RESEARCH.md
@apps/api/src/config/session.ts
@apps/api/src/routes/auth.ts
@apps/api/src/middleware/auth.ts
@apps/web/src/app/login/page.tsx
@apps/web/src/app/register/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden API auth/session lifecycle for fail-safe session behavior</name>
  <files>apps/api/src/config/session.ts, apps/api/src/index.ts, apps/api/src/routes/auth.ts, apps/api/src/middleware/auth.ts, apps/api/src/lib/validation/auth.ts</files>
  <action>Implement schema-based request validation for register/login payloads (zod), normalize emails to lowercase, enforce password policy, and keep generic invalid-credentials responses. Regenerate session IDs on register/login before setting user identity to prevent fixation, persist session explicitly, and ensure logout destroys the server-side session and clears cookie state. Configure secure cookie/session behavior for production behind proxy-safe settings while preserving local SaaS defaults from Phase 1.</action>
  <verify>Run `npm --prefix apps/api run build` and exercise `POST /auth/register`, `POST /auth/login`, `GET /auth/me`, `POST /auth/logout` with curl or HTTP client; verify 401 on `/auth/me` after logout.</verify>
  <done>Register/login establish authenticated session, `/auth/me` returns current user when authenticated, invalid credentials return 401 without account enumeration, and logout invalidates access immediately.</done>
</task>

<task type="auto">
  <name>Task 2: Align web auth screens with hardened session contracts</name>
  <files>apps/web/src/lib/api.ts, apps/web/src/app/login/page.tsx, apps/web/src/app/register/page.tsx, apps/web/src/app/page.tsx</files>
  <action>Update web auth flows to match API validation and error contracts, including normalized error rendering and deterministic redirects after successful auth. Keep cookie-based requests with `credentials: include`, add explicit authenticated-session restore on app entry, and wire logout to the hardened endpoint so stale UI state clears immediately.</action>
  <verify>Run `npm --prefix apps/web run build`; manually test register -> authenticated landing, logout -> redirected unauthenticated view, and failed login shows clear error without exposing internals.</verify>
  <done>Web login/register/logout behavior is consistent with API auth contracts and authenticated session continuity works across refresh and navigation.</done>
</task>

</tasks>

<verification>
Build both API and web apps, then run one end-to-end auth smoke flow: register, refresh, login, logout, and verify protected route denial after logout.</verification>

<success_criteria>
AUTH-01 through AUTH-04 are satisfied with secure server-side sessions, deterministic auth UX, and no session fixation/logout invalidation regressions.</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-access-and-tenant-isolation/02-01-SUMMARY.md`
</output>
