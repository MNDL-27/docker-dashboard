---
phase: 02-identity-access-and-tenant-isolation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/src/authz/roleMatrix.ts
  - apps/api/src/middleware/scope.ts
  - apps/api/src/routes/organizations.ts
  - apps/api/src/routes/projects.ts
  - apps/api/src/routes/invites.ts
  - apps/web/src/app/page.tsx
  - apps/web/src/app/onboarding/organization/page.tsx
  - apps/web/src/app/(dashboard)/fleet/page.tsx
  - apps/web/src/lib/api.ts
autonomous: true
requirements:
  - TEN-01
  - TEN-02
must_haves:
  truths:
    - Authenticated user can create a new organization and becomes OWNER of that organization.
    - Authenticated organization member can create projects only inside organizations they belong to.
    - Organization and project list responses only show scopes tied to the current user.
  artifacts:
    - path: apps/api/src/routes/organizations.ts
      provides: Organization create/list/read endpoints with owner bootstrap membership.
      exports: [default]
    - path: apps/api/src/routes/projects.ts
      provides: Org-scoped project create/list/read endpoints with role gates.
      exports: [default]
    - path: apps/web/src/app/onboarding/organization/page.tsx
      provides: First-organization creation flow for newly registered users.
  key_links:
    - from: apps/api/src/routes/projects.ts
      to: apps/api/src/middleware/scope.ts
      via: mandatory organization membership resolution before project queries.
      pattern: requireOrgContext|resolveOrgScope
    - from: apps/web/src/app/(dashboard)/fleet/page.tsx
      to: /api/organizations/:orgId/projects
      via: project list and project create requests tied to selected organization.
      pattern: /api/organizations/\$\{orgId\}/projects
---

<objective>
Implement tenant scope foundations for organizations and projects so users can create and operate in explicit boundaries.

Purpose: Deliver first-class org/project segmentation that future host and observability features can trust.
Output: Consolidated org/project authorization path in API and onboarding/dashboard UX for organization + project creation.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-access-and-tenant-isolation/CONTEXT.md
@.planning/phases/02-identity-access-and-tenant-isolation/02-RESEARCH.md
@.planning/phases/02-identity-access-and-tenant-isolation/02-01-SUMMARY.md
@apps/api/src/routes/organizations.ts
@apps/api/src/routes/projects.ts
@apps/web/src/app/(dashboard)/fleet/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add centralized org/project scope resolution and role matrix helpers</name>
  <files>apps/api/src/authz/roleMatrix.ts, apps/api/src/middleware/scope.ts, apps/api/src/routes/organizations.ts, apps/api/src/routes/projects.ts</files>
  <action>Create a single role matrix helper for Owner/Admin/Operator/Viewer checks and reusable middleware that resolves auth context `{userId, organizationId, role}` fail-closed before handlers run. Refactor organization/project routes to consume shared scope helpers (no ad-hoc inline role branching), enforce organization-scoped queries in primary Prisma `where` clauses, and preserve existing route contracts where possible to avoid web breakage.</action>
  <verify>Run `npm --prefix apps/api run build` and verify `GET /organizations`, `POST /organizations`, `GET /organizations/:orgId/projects`, and `POST /organizations/:orgId/projects` with member and non-member users; non-members must receive 403.</verify>
  <done>Organization/project CRUD paths consistently use centralized scope + role checks and deny missing/out-of-scope context by default.</done>
</task>

<task type="auto">
  <name>Task 2: Add organization onboarding and scoped project creation UX</name>
  <files>apps/web/src/app/page.tsx, apps/web/src/app/onboarding/organization/page.tsx, apps/web/src/app/(dashboard)/fleet/page.tsx, apps/web/src/lib/api.ts</files>
  <action>Implement a first-run onboarding flow for authenticated users with zero organizations and route them to create an organization before accessing dashboard features. Update fleet dashboard org/project interactions to use selected organization context explicitly, keep UX low-friction (no broad redesign), and surface authorization failures with clear scope-focused messaging.</action>
  <verify>Run `npm --prefix apps/web run build`; test with a new account (no org) and an existing account (has org). New account should be guided to organization creation, then able to create a project inside that org.</verify>
  <done>User can create organization and project from web flow, and project APIs never operate outside selected/member organization scope.</done>
</task>

</tasks>

<verification>
Execute org/project API checks for member vs non-member users and validate onboarding path from zero-org state to first project creation.</verification>

<success_criteria>
TEN-01 and TEN-02 are met with explicit organization/project boundaries, role-aware creation permissions, and fail-closed org scope enforcement.</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-access-and-tenant-isolation/02-02-SUMMARY.md`
</output>
