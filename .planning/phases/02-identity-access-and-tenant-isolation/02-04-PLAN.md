---
phase: 02-identity-access-and-tenant-isolation
plan: 04
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
files_modified:
  - apps/api/src/middleware/scope.ts
  - apps/api/src/services/scopedAccess.ts
  - apps/api/src/routes/hosts.ts
  - apps/api/src/routes/actions.ts
  - apps/api/src/routes/audit.ts
  - apps/api/src/routes/agent.ts
autonomous: true
requirements:
  - TEN-05
must_haves:
  truths:
    - User can only read/write records for organizations and projects where they are a member.
    - Cross-tenant access attempts to hosts, containers, actions, audit, and ingest routes are denied quickly.
    - Missing tenant context does not default to broad access; requests fail closed.
    - Agent ingest rejects out-of-scope organization/project context using the same scoped access rules as HTTP routes.
  artifacts:
    - path: apps/api/src/services/scopedAccess.ts
      provides: Shared scoped query utilities used by non-identity routes.
    - path: apps/api/src/middleware/scope.ts
      provides: Reusable fail-closed tenant context enforcement middleware.
    - path: apps/api/src/routes/agent.ts
      provides: Ingest route tenant/membership validation before accepting telemetry payloads.
  key_links:
    - from: apps/api/src/routes/hosts.ts
      to: apps/api/src/services/scopedAccess.ts
      via: all host/container queries include organization/project constraints.
      pattern: organizationId|projectId
    - from: apps/api/src/routes/agent.ts
      to: apps/api/src/services/scopedAccess.ts
      via: ingest path resolves and verifies org/project membership before data is accepted.
      pattern: scopedAccess|resolveOrgScope|organizationId|projectId
---

<objective>
Enforce strict tenant isolation across all active API/data access paths so out-of-scope data is never visible or mutable.

Purpose: Close the security boundary for current and forward-compatible routes before shipping Phase 2 as complete.
Output: Shared scoped-access guardrails applied to core non-identity HTTP and ingest paths.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-access-and-tenant-isolation/CONTEXT.md
@.planning/phases/02-identity-access-and-tenant-isolation/02-RESEARCH.md
@.planning/phases/02-identity-access-and-tenant-isolation/02-02-SUMMARY.md
@.planning/phases/02-identity-access-and-tenant-isolation/02-03-SUMMARY.md
@apps/api/src/routes/hosts.ts
@apps/api/src/routes/actions.ts
@apps/api/src/routes/audit.ts
@apps/api/src/routes/agent.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce reusable scoped-access primitives and wire core data routes</name>
  <files>apps/api/src/middleware/scope.ts, apps/api/src/services/scopedAccess.ts, apps/api/src/routes/hosts.ts, apps/api/src/routes/actions.ts, apps/api/src/routes/audit.ts</files>
  <action>Create shared scoped-access helpers that resolve `{userId, organizationId, projectId, role}` fail-closed and use them in hosts/actions/audit handlers. Replace check-after-fetch patterns with organization/project-constrained primary queries, and keep endpoint response shapes stable unless required to prevent leakage.</action>
  <verify>Run `npm --prefix apps/api run build` and verify member vs non-member access on hosts/actions/audit endpoints; non-members and mismatched scope IDs must return 403/404 with no out-of-scope records.</verify>
  <done>Hosts/actions/audit routes all enforce query-bound tenant constraints through shared scope primitives and deny unresolved scope by default.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce ingest-path tenant scope using shared scoped-access service</name>
  <files>apps/api/src/routes/agent.ts</files>
  <action>Apply the same scoped-access validation path to ingest traffic in `agent.ts`: resolve organization/project scope from authenticated ingest context, verify membership/authorization with shared helpers before processing payloads, and fail closed on missing or mismatched scope. Do not introduce Phase 3 enrollment-token behavior; keep this limited to tenant boundary enforcement.</action>
  <verify>Exercise ingest route requests with valid in-tenant scope and cross-tenant/missing scope; valid scope is accepted while invalid scope consistently returns 403/404 without processing data.</verify>
  <done>Ingest route cannot accept out-of-scope organization/project data and follows the same fail-closed scope model as core HTTP routes.</done>
</task>

</tasks>

<verification>
Run API build and execute hostile-scope checks for hosts/actions/audit/ingest endpoints to confirm out-of-scope IDs always fail and return no tenant data.</verification>

<success_criteria>
TEN-05 is met with fail-closed tenant enforcement across core HTTP and ingest paths using centralized scoped-access wiring.</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-access-and-tenant-isolation/02-04-SUMMARY.md`
</output>
