---
phase: 02-identity-access-and-tenant-isolation
plan: 04
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
files_modified:
  - apps/api/src/middleware/scope.ts
  - apps/api/src/services/scopedAccess.ts
  - apps/api/src/routes/hosts.ts
  - apps/api/src/routes/actions.ts
  - apps/api/src/routes/audit.ts
  - apps/api/src/routes/alerts.ts
  - apps/api/src/routes/webhooks.ts
  - apps/api/src/websocket/auth.ts
  - apps/api/src/routes/agent.ts
  - apps/api/src/index.ts
  - apps/api/src/__tests__/tenant-isolation.test.ts
  - apps/api/package.json
autonomous: true
requirements:
  - TEN-05
  - SECU-01
must_haves:
  truths:
    - User can only read/write records for organizations and projects where they are a member.
    - Cross-tenant access attempts to hosts, containers, actions, audit, alerts, and webhooks are denied quickly.
    - Missing tenant context does not default to broad access; requests fail closed.
    - Websocket and ingest paths enforce the same tenant boundaries as HTTP routes.
  artifacts:
    - path: apps/api/src/services/scopedAccess.ts
      provides: Shared scoped query utilities used by non-identity routes.
    - path: apps/api/src/middleware/scope.ts
      provides: Reusable fail-closed tenant context enforcement middleware.
    - path: apps/api/src/__tests__/tenant-isolation.test.ts
      provides: Negative cross-tenant regression coverage for primary API surfaces.
  key_links:
    - from: apps/api/src/routes/hosts.ts
      to: apps/api/src/services/scopedAccess.ts
      via: all host/container queries include organization/project constraints.
      pattern: organizationId|projectId
    - from: apps/api/src/websocket/auth.ts
      to: apps/api/src/middleware/scope.ts
      via: handshake/session tenant validation before stream subscription.
      pattern: organization|tenant|scope
---

<objective>
Enforce strict tenant isolation across all active API/data access paths so out-of-scope data is never visible or mutable.

Purpose: Close the security boundary for current and forward-compatible routes before shipping Phase 2 as complete.
Output: Shared scoped-access guardrails applied to API + websocket paths, plus cross-tenant regression tests.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-access-and-tenant-isolation/CONTEXT.md
@.planning/phases/02-identity-access-and-tenant-isolation/02-RESEARCH.md
@.planning/phases/02-identity-access-and-tenant-isolation/02-02-SUMMARY.md
@.planning/phases/02-identity-access-and-tenant-isolation/02-03-SUMMARY.md
@apps/api/src/routes/hosts.ts
@apps/api/src/routes/actions.ts
@apps/api/src/routes/audit.ts
@apps/api/src/routes/alerts.ts
@apps/api/src/routes/webhooks.ts
@apps/api/src/websocket/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply fail-closed scoped access helpers across non-identity API routes</name>
  <files>apps/api/src/middleware/scope.ts, apps/api/src/services/scopedAccess.ts, apps/api/src/routes/hosts.ts, apps/api/src/routes/actions.ts, apps/api/src/routes/audit.ts, apps/api/src/routes/alerts.ts, apps/api/src/routes/webhooks.ts, apps/api/src/websocket/auth.ts, apps/api/src/routes/agent.ts, apps/api/src/index.ts</files>
  <action>Introduce a shared scoped access service and require it in every route/websocket handler that touches tenant-owned data. Remove any check-after-fetch patterns by applying organization/project constraints directly in primary queries, deny when scope cannot be resolved, and ensure response payloads never include out-of-scope entities. Keep behavior compatible with current endpoint contracts where possible, but prioritize fail-closed isolation if a conflict appears.</action>
  <verify>Run `npm --prefix apps/api run build` and exercise representative endpoints with mismatched `organizationId`/`projectId` (or cross-tenant resource IDs); each must return 403/404 without data leakage.</verify>
  <done>All active API and websocket data paths enforce tenant scope at query boundary and fail closed when membership/scope is missing.</done>
</task>

<task type="auto">
  <name>Task 2: Add tenant-isolation regression coverage for primary API surfaces</name>
  <files>apps/api/src/__tests__/tenant-isolation.test.ts, apps/api/package.json</files>
  <action>Add an automated tenant-isolation test suite that seeds at least two organizations and verifies negative cases for hosts, projects, invites/member operations, alerts/webhooks/actions/audit, and websocket auth handshakes. Add or update a runnable API test script to execute this suite in CI/local without requiring manual assertions.</action>
  <verify>Run the new test command (for example `npm --prefix apps/api run test`) and confirm cross-tenant negative cases pass while in-tenant control cases remain green.</verify>
  <done>SECU-01 and TEN-05 are guarded by repeatable automated tests that fail on cross-tenant leakage regressions.</done>
</task>

</tasks>

<verification>
Run API build plus tenant-isolation tests, then perform one manual hostile-scope smoke check against a protected endpoint to confirm no out-of-scope data is returned.</verification>

<success_criteria>
TEN-05 and SECU-01 are met with fail-closed tenant enforcement across HTTP/websocket/data paths and automated regression coverage for leakage attempts.</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-access-and-tenant-isolation/02-04-SUMMARY.md`
</output>
