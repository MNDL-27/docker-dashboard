---
phase: 02-identity-access-and-tenant-isolation
plan: 05
type: execute
wave: 5
depends_on:
  - 02-04
files_modified:
  - apps/api/src/routes/alerts.ts
  - apps/api/src/routes/webhooks.ts
  - apps/api/src/websocket/auth.ts
  - apps/api/src/index.ts
  - apps/api/src/__tests__/tenant-isolation.test.ts
  - apps/api/package.json
autonomous: true
requirements:
  - SECU-01
must_haves:
  truths:
    - Cross-tenant access attempts through alerts, webhooks, and websocket subscription/auth paths are denied quickly.
    - Realtime/auth surfaces fail closed when organization/project scope is missing or mismatched.
    - Automated tests catch cross-tenant leakage regressions on core non-identity API surfaces.
  artifacts:
    - path: apps/api/src/routes/alerts.ts
      provides: Alert routes constrained by tenant-scoped access helpers.
    - path: apps/api/src/routes/webhooks.ts
      provides: Webhook route handling constrained to in-scope tenant entities.
    - path: apps/api/src/websocket/auth.ts
      provides: Socket handshake tenant validation before stream subscription.
    - path: apps/api/src/__tests__/tenant-isolation.test.ts
      provides: Negative cross-tenant regression suite for HTTP and realtime ingest/auth surfaces.
  key_links:
    - from: apps/api/src/websocket/auth.ts
      to: apps/api/src/middleware/scope.ts
      via: handshake/session tenant validation reuses fail-closed scope resolution.
      pattern: organization|tenant|scope|requireOrgContext
    - from: apps/api/src/routes/alerts.ts
      to: apps/api/src/services/scopedAccess.ts
      via: alert queries include organization/project constraints at query boundary.
      pattern: scopedAccess|organizationId|projectId
---

<objective>
Complete tenant isolation enforcement for remaining realtime/notification surfaces and lock it in with automated leakage regression tests.

Purpose: Close SECU-01 for all active non-identity access paths after core scoped-access wiring lands.
Output: Alerts/webhooks/websocket tenant-boundary hardening plus runnable cross-tenant regression suite.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-access-and-tenant-isolation/CONTEXT.md
@.planning/phases/02-identity-access-and-tenant-isolation/02-RESEARCH.md
@.planning/phases/02-identity-access-and-tenant-isolation/02-04-SUMMARY.md
@apps/api/src/routes/alerts.ts
@apps/api/src/routes/webhooks.ts
@apps/api/src/websocket/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply fail-closed scoped access to alerts, webhooks, and websocket auth paths</name>
  <files>apps/api/src/routes/alerts.ts, apps/api/src/routes/webhooks.ts, apps/api/src/websocket/auth.ts, apps/api/src/index.ts</files>
  <action>Refactor alerts/webhooks routes and websocket auth/handshake flow to consume scoped-access + scope middleware from earlier plans instead of route-local authorization branches. Ensure organization/project constraints are applied in primary lookups, deny unresolved scope early, and keep current route/socket contracts unless a contract must tighten to enforce fail-closed behavior.</action>
  <verify>Run `npm --prefix apps/api run build` and test cross-tenant IDs against alerts/webhooks endpoints plus websocket connect attempts with mismatched scope; all out-of-scope attempts must be rejected with no payload leakage.</verify>
  <done>Remaining realtime/notification paths enforce the same tenant boundary guarantees as core HTTP routes.</done>
</task>

<task type="auto">
  <name>Task 2: Add automated cross-tenant regression suite for hardened non-identity surfaces</name>
  <files>apps/api/src/__tests__/tenant-isolation.test.ts, apps/api/package.json</files>
  <action>Implement or extend tenant-isolation tests to seed at least two organizations and assert negative cross-tenant behavior for hosts/actions/audit, alerts/webhooks, ingest, and websocket auth. Add/update a deterministic API test script in package.json so this suite runs in local and CI without manual setup steps beyond existing test prerequisites.</action>
  <verify>Run the API tenant-isolation test command (for example `npm --prefix apps/api run test`) and confirm all negative cross-tenant cases pass while in-tenant controls remain green.</verify>
  <done>SECU-01 is continuously protected by automated regression coverage across HTTP/realtime/ingest access paths.</done>
</task>

</tasks>

<verification>
Run API build and tenant-isolation test suite, then execute one manual websocket or webhook hostile-scope smoke check to confirm rejection behavior matches automated expectations.</verification>

<success_criteria>
SECU-01 is satisfied by fail-closed enforcement on remaining realtime/notification surfaces and repeatable tests that fail on leakage regressions.</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-access-and-tenant-isolation/02-05-SUMMARY.md`
</output>
