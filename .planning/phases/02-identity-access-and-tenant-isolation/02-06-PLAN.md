---
phase: 02-identity-access-and-tenant-isolation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/(dashboard)/layout.tsx
  - apps/web/src/components/navigation/DashboardShell.tsx
  - apps/web/src/lib/api.ts
autonomous: true
requirements:
  - AUTH-04
must_haves:
  truths:
    - Authenticated users can trigger logout from primary dashboard navigation without visiting a non-dashboard page.
    - Logout clears client-selected tenant scope state before redirecting to /login.
    - After logout, protected dashboard routes no longer render authenticated content and return to login flow.
  artifacts:
    - path: apps/web/src/app/(dashboard)/layout.tsx
      provides: Shared dashboard shell mounted across authenticated dashboard routes.
    - path: apps/web/src/components/navigation/DashboardShell.tsx
      provides: Primary dashboard navigation links with reachable sign-out control.
    - path: apps/web/src/lib/api.ts
      provides: Reusable logout and scope-clear helpers consumed by dashboard shell.
  key_links:
    - from: apps/web/src/components/navigation/DashboardShell.tsx
      to: apps/web/src/lib/api.ts
      via: sign-out handler calls logout() then clearSelectedOrganizationId().
      pattern: logout\(|clearSelectedOrganizationId\(
    - from: apps/web/src/components/navigation/DashboardShell.tsx
      to: /login
      via: router replace after successful or failed logout attempt.
      pattern: router\.(replace|push)\('/login'
    - from: apps/web/src/app/(dashboard)/layout.tsx
      to: apps/web/src/components/navigation/DashboardShell.tsx
      via: route-group layout wraps all dashboard pages in primary navigation shell.
      pattern: DashboardShell
gap_closure: true
---

<objective>
Close the Phase 2 verification gap by wiring a reachable, user-initiated logout flow into the authenticated dashboard shell.

Purpose: Fully satisfy AUTH-04 in web UX so session invalidation is accessible from the primary dashboard navigation path.
Output: Shared dashboard navigation shell with sign-out action that calls logout(), clears client tenant scope state, and redirects to /login.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-identity-access-and-tenant-isolation/CONTEXT.md
@.planning/phases/02-identity-access-and-tenant-isolation/02-VERIFICATION.md
@.planning/phases/02-identity-access-and-tenant-isolation/02-01-SUMMARY.md
@apps/web/src/lib/api.ts
@apps/web/src/app/(dashboard)/fleet/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared dashboard shell with primary navigation coverage</name>
  <files>apps/web/src/app/(dashboard)/layout.tsx, apps/web/src/components/navigation/DashboardShell.tsx</files>
  <action>Create a route-group layout for `app/(dashboard)` that renders a shared dashboard shell around existing dashboard pages. The shell must expose primary navigation links for current dashboard destinations (fleet, alerts, audit, and settings surfaces already present in this phase) so sign-out is reachable from the same persistent nav across authenticated pages. Keep existing page content and route paths intact; do not perform a broad visual redesign.</action>
  <verify>Run `npm --prefix apps/web run build` and manually open at least `/fleet` and `/alerts` to confirm the shared primary navigation renders consistently and links remain functional.</verify>
  <done>All dashboard routes under `app/(dashboard)` render inside one primary navigation shell where sign-out can be surfaced globally.</done>
</task>

<task type="auto">
  <name>Task 2: Wire sign-out control to logout, scope clear, and redirect</name>
  <files>apps/web/src/components/navigation/DashboardShell.tsx, apps/web/src/lib/api.ts</files>
  <action>Implement a mounted sign-out control in the shared dashboard shell that calls `logout()` from `apps/web/src/lib/api.ts`, clears client organization scope state via `clearSelectedOrganizationId()`, and redirects to `/login` using Next navigation after the action completes. Ensure cleanup and redirect occur even if logout API returns an error so stale client scope is never retained after user-initiated sign-out. Keep behavior deterministic and low-friction per phase UX constraints.</action>
  <verify>With `npm --prefix apps/web run dev`, authenticate, click sign-out from dashboard navigation, confirm browser lands on `/login`, then navigate to `/fleet` and verify unauthenticated redirect behavior prevents dashboard access.</verify>
  <done>User-triggered sign-out is reachable from primary dashboard navigation and cleanly exits authenticated context (server logout + client scope cleared + login redirect).</done>
</task>

</tasks>

<verification>
Execute web build plus end-to-end browser smoke flow: login -> navigate dashboard pages -> sign out from primary nav -> verify redirect to `/login` and blocked re-entry to protected dashboard routes without re-authentication.
</verification>

<success_criteria>
The failed verification truth is now fully met: users can log out cleanly from a reachable dashboard navigation control, and authenticated dashboard state is cleared and inaccessible until login resumes.</success_criteria>

<output>
After completion, create `.planning/phases/02-identity-access-and-tenant-isolation/02-06-SUMMARY.md`
</output>
