---
phase: 03-host-enrollment-and-trusted-connectivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - apps/api/src/services/enrollment.ts
  - apps/api/src/routes/hosts.ts
  - apps/api/src/__tests__/host-enrollment-token.test.ts
  - apps/web/src/components/fleet/AddHostDialog.tsx
  - apps/web/src/lib/api.ts
autonomous: true
requirements: [ENRL-01, ENRL-02]
must_haves:
  truths:
    - "Operator can generate a one-time enrollment token for a selected org/project host enrollment intent."
    - "Operator sees a copy-ready install snippet with cloud URL and enrollment token from API response."
    - "Enrollment bootstrap secret is short-lived and stored server-side in protected form only."
  artifacts:
    - path: "apps/api/src/routes/hosts.ts"
      provides: "Tenant-scoped host enrollment intent endpoint and snippet response"
      exports: ["default"]
    - path: "apps/api/src/services/enrollment.ts"
      provides: "Bootstrap token generation and hashing helpers"
      contains: "issueEnrollmentToken"
    - path: "apps/web/src/components/fleet/AddHostDialog.tsx"
      provides: "UI for enrollment token generation and install command copy"
      min_lines: 120
  key_links:
    - from: "apps/web/src/components/fleet/AddHostDialog.tsx"
      to: "/api/hosts/tokens"
      via: "apiFetch POST"
      pattern: "apiFetch.*hosts/tokens"
    - from: "apps/api/src/routes/hosts.ts"
      to: "apps/api/src/services/enrollment.ts"
      via: "enrollment token issuance"
      pattern: "issueEnrollmentToken"
---

<objective>
Implement secure host enrollment intent creation so operators can create enrollment records and receive a copy-ready install command containing cloud URL and one-time token.

Purpose: Satisfies the enrollment entrypoint loop (ENRL-01/02) and establishes secure bootstrap token handling before first agent connect.
Output: Tenant-scoped host token issuance API, protected token storage primitives, and updated fleet enrollment dialog command rendering.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-host-enrollment-and-trusted-connectivity/CONTEXT.md
@.planning/phases/03-host-enrollment-and-trusted-connectivity/03-RESEARCH.md
@apps/api/src/routes/hosts.ts
@apps/web/src/components/fleet/AddHostDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tenant-scoped enrollment intent issuance with protected bootstrap secrets</name>
  <files>prisma/schema.prisma, apps/api/src/services/enrollment.ts, apps/api/src/routes/hosts.ts, apps/api/src/__tests__/host-enrollment-token.test.ts</files>
  <action>Add an enrollment issuance service that generates 32-byte CSPRNG bootstrap secrets, stores only a SHA-256 hash + metadata/expiry in `HostToken`, and returns plaintext token only in the creation response. Update `POST /hosts/tokens` to remain scoped through `resolveUserScope`, require OWNER/ADMIN/OPERATOR, and create the host enrollment intent and token in one transaction. Keep outbound-only model intact (no inbound host callbacks) and explicitly avoid plaintext token persistence anywhere in Prisma writes or logs.</action>
  <verify>npm --prefix apps/api run test -- host-enrollment-token.test.ts && npm --prefix apps/api run build</verify>
  <done>Token issuance is scoped to org/project membership, token TTL is enforced in persisted metadata, and tests prove raw token value is not persisted while API still returns one copy-safe bootstrap token.</done>
</task>

<task type="auto">
  <name>Task 2: Update fleet enrollment dialog to show deterministic install snippet</name>
  <files>apps/web/src/components/fleet/AddHostDialog.tsx, apps/web/src/lib/api.ts</files>
  <action>Update Add Host flow to call the hardened token endpoint, render token expiry and project context, and display a copy-ready install command using API-provided cloud URL + token. Keep UX explicit/trustworthy per context: clear step labels, single command block, and success/error states without adding out-of-scope fleet inventory or telemetry features.</action>
  <verify>npm --prefix apps/web run lint</verify>
  <done>Operator can generate a token and copy one install command that includes cloud URL and token for the selected organization/project scope.</done>
</task>

</tasks>

<verification>
Validate `POST /hosts/tokens` with an in-scope and out-of-scope user, confirm response includes command/token/expiry, and confirm DB stores only hashed bootstrap token fields.
</verification>

<success_criteria>
- ENRL-01: Host enrollment intent and one-time token can be created from authenticated fleet UX for scoped org/project.
- ENRL-02: Install snippet is visible and copy-ready with cloud URL + token payload from API.
</success_criteria>

<output>
After completion, create `.planning/phases/03-host-enrollment-and-trusted-connectivity/03-host-enrollment-and-trusted-connectivity-01-SUMMARY.md`
</output>
