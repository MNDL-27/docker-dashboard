---
phase: 03-host-enrollment-and-trusted-connectivity
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/api/src/services/enrollment.ts
  - apps/api/src/routes/agent.ts
  - apps/api/src/middleware/agentAuth.ts
  - apps/api/src/websocket/auth.ts
  - apps/api/src/__tests__/agent-enrollment-exchange.test.ts
autonomous: true
requirements: [ENRL-03]
must_haves:
  truths:
    - "Platform exchanges a valid first-connect enrollment token for durable agent credentials exactly once."
    - "Invalid, expired, or replayed enrollment attempts are rejected with deterministic non-200 responses."
    - "Durable agent credentials are bound to host, organization, and project scope used by ingest and websocket auth."
  artifacts:
    - path: "apps/api/src/routes/agent.ts"
      provides: "First-connect enrollment exchange endpoint"
      exports: ["default"]
    - path: "apps/api/src/services/enrollment.ts"
      provides: "Atomic compare-and-consume token exchange + durable credential issuance"
      contains: "consumeEnrollmentToken"
    - path: "apps/api/src/middleware/agentAuth.ts"
      provides: "Strict durable credential validation with tenant/host binding"
      contains: "requireAgentAuth"
  key_links:
    - from: "apps/api/src/routes/agent.ts"
      to: "apps/api/src/services/enrollment.ts"
      via: "POST /agent/enroll exchange"
      pattern: "consumeEnrollmentToken"
    - from: "apps/api/src/middleware/agentAuth.ts"
      to: "apps/api/src/websocket/auth.ts"
      via: "shared JWT claim validation rules"
      pattern: "audience|issuer|algorithms"
---

<objective>
Implement the first-connect enrollment exchange so one-time bootstrap tokens are atomically consumed and replaced with durable, tenant-bound agent credentials.

Purpose: Closes ENRL-03 trust boundary by preventing replay enrollment and ensuring all agent transports share the same credential validation policy.
Output: Atomic enrollment consume flow, strict durable credential issuance/verification, and regression tests for valid/invalid/replayed scenarios.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-host-enrollment-and-trusted-connectivity/CONTEXT.md
@.planning/phases/03-host-enrollment-and-trusted-connectivity/03-RESEARCH.md
@apps/api/src/routes/agent.ts
@apps/api/src/middleware/agentAuth.ts
@apps/api/src/websocket/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace read-then-update enrollment with atomic compare-and-consume exchange</name>
  <files>apps/api/src/services/enrollment.ts, apps/api/src/routes/agent.ts, apps/api/src/__tests__/agent-enrollment-exchange.test.ts</files>
  <action>Implement `POST /agent/enroll` using a single transaction that conditionally consumes an unused, unexpired bootstrap token hash (`usedAt: null`, `expiresAt > now`) and only then issues durable agent credentials. Reject invalid/expired/replayed attempts consistently, and prevent duplicate host enrollment under concurrent requests by requiring exactly one token-consume row update. Keep implementation tenant-scoped and aligned with Phase 2 scopedAccess checks.</action>
  <verify>npm --prefix apps/api run test -- agent-enrollment-exchange.test.ts && npm --prefix apps/api run build</verify>
  <done>Valid token can enroll exactly once and returns durable credentials; replay and invalid token paths are rejected and covered by deterministic tests.</done>
</task>

<task type="auto">
  <name>Task 2: Harden durable credential validation across HTTP and websocket agent auth</name>
  <files>apps/api/src/middleware/agentAuth.ts, apps/api/src/websocket/auth.ts, apps/api/src/__tests__/agent-enrollment-exchange.test.ts</files>
  <action>Enforce explicit JWT verification constraints (algorithm, issuer, audience, expiration) and require host/org/project claim binding against persisted host scope in both HTTP middleware and websocket auth path. Add regression assertions for mismatched scope claims and unsupported token profiles to prevent policy drift between transports.</action>
  <verify>npm --prefix apps/api run test -- agent-enrollment-exchange.test.ts && npm --prefix apps/api run build</verify>
  <done>HTTP ingest and websocket authentication accept only correctly-scoped durable credentials and reject mismatched claims deterministically.</done>
</task>

</tasks>

<verification>
Run enrollment exchange tests with concurrent replay attempts and scope mismatch cases; confirm only first valid consume succeeds and durable credential validation is identical for HTTP and websocket auth.
</verification>

<success_criteria>
- ENRL-03: First-connect exchange validates one-time token, consumes it once, and issues durable credentials.
- ENRL-03: Invalid/replayed enrollment attempts fail deterministically and do not create additional host enrollments.
</success_criteria>

<output>
After completion, create `.planning/phases/03-host-enrollment-and-trusted-connectivity/03-host-enrollment-and-trusted-connectivity-02-SUMMARY.md`
</output>
