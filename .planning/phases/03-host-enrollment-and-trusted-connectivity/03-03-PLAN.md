---
phase: 03-host-enrollment-and-trusted-connectivity
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - apps/api/src/services/presence.ts
  - apps/api/src/routes/hosts.ts
  - apps/api/src/routes/agent.ts
  - apps/api/src/middleware/rateLimit.ts
  - apps/api/src/index.ts
  - apps/api/src/__tests__/host-presence-and-rate-limit.test.ts
  - apps/web/src/components/fleet/HostList.tsx
autonomous: true
requirements: [ENRL-04, ENRL-05, SECU-03]
must_haves:
  truths:
    - "Host online/offline status is derived from heartbeat freshness using one deterministic policy."
    - "Fleet UI shows host connectivity status and last-seen timestamp from API source-of-truth."
    - "Per-host rate limits throttle abusive API/ingest traffic without globally blocking healthy hosts."
  artifacts:
    - path: "apps/api/src/services/presence.ts"
      provides: "Canonical heartbeat freshness policy and status derivation"
      contains: "deriveHostConnectivity"
    - path: "apps/api/src/middleware/rateLimit.ts"
      provides: "Host-keyed limiter policies for enroll/heartbeat/ingest/UI routes"
      contains: "createAgentRateLimiters"
    - path: "apps/web/src/components/fleet/HostList.tsx"
      provides: "Connectivity and last-seen rendering from API host payload"
      min_lines: 140
  key_links:
    - from: "apps/api/src/routes/agent.ts"
      to: "apps/api/src/services/presence.ts"
      via: "heartbeat updates and status derivation"
      pattern: "deriveHostConnectivity|recordHeartbeat"
    - from: "apps/api/src/index.ts"
      to: "apps/api/src/middleware/rateLimit.ts"
      via: "route-level limiter application"
      pattern: "rateLimit"
---

<objective>
Centralize heartbeat-derived presence and enforce host-aware throttling so operators can trust connectivity status while abusive traffic is isolated per host.

Purpose: Completes ENRL-04/05 and SECU-03 with deterministic online/offline behavior and guarded API/ingest endpoints.
Output: Shared presence service, host status/last-seen API + UI alignment, host-keyed rate limit middleware, and regression coverage.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-host-enrollment-and-trusted-connectivity/CONTEXT.md
@.planning/phases/03-host-enrollment-and-trusted-connectivity/03-RESEARCH.md
@apps/api/src/routes/hosts.ts
@apps/api/src/routes/agent.ts
@apps/web/src/components/fleet/HostList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make heartbeat freshness a single source of truth for host connectivity</name>
  <files>apps/api/src/services/presence.ts, apps/api/src/routes/hosts.ts, apps/api/src/routes/agent.ts, apps/api/src/__tests__/host-presence-and-rate-limit.test.ts</files>
  <action>Create a shared presence service defining one configurable freshness threshold (for example `HOST_ONLINE_THRESHOLD_MS`) and use it for all host list/detail responses and heartbeat writes. Remove duplicated inline status calculations in routes, normalize API payload to include `status` and `lastSeen`, and keep all host reads tenant-scoped through existing scoped access helpers. Document threshold behavior in code constants to keep transitions predictable.</action>
  <verify>npm --prefix apps/api run test -- host-presence-and-rate-limit.test.ts && npm --prefix apps/api run build</verify>
  <done>Host status transitions (online/offline) are computed consistently by one service and exposed with last-seen timestamps on host APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-host limiter middleware for API and ingest endpoints</name>
  <files>apps/api/src/middleware/rateLimit.ts, apps/api/src/index.ts, apps/api/src/routes/agent.ts, apps/api/src/routes/hosts.ts, apps/api/src/__tests__/host-presence-and-rate-limit.test.ts</files>
  <action>Add `express-rate-limit` policies with separate quotas for enrollment bootstrap, heartbeat, and container ingest. Key authenticated agent traffic by `req.agent.hostId`, and use a deterministic fallback key for pre-auth enrollment traffic (token hash or IP) so one abusive host does not throttle healthy hosts. Wire middleware at route mount points in `index.ts`; keep Redis-store support env-gated for multi-instance production while preserving in-memory default for local/dev.</action>
  <verify>npm --prefix apps/api run test -- host-presence-and-rate-limit.test.ts && npm --prefix apps/api run build</verify>
  <done>Rate limits are enforced on target routes with host-aware buckets, abusive clients receive throttled responses, and non-abusive hosts continue succeeding under test.</done>
</task>

<task type="auto">
  <name>Task 3: Align fleet UI host cards/table with API connectivity source-of-truth</name>
  <files>apps/web/src/components/fleet/HostList.tsx</files>
  <action>Render host connectivity status badges and last-seen timestamps directly from API fields without client-side fallback recomputation. Preserve current fleet scope behavior from Phase 2 and avoid adding out-of-scope inventory filters/search; this task is only status clarity and trust alignment for enrolled hosts.</action>
  <verify>npm --prefix apps/web run lint</verify>
  <done>Fleet host list clearly shows online/offline and last-seen based on API-provided presence values, matching ENRL-05 expectations.</done>
</task>

</tasks>

<verification>
Confirm stale heartbeat hosts transition to offline after freshness window, confirm healthy heartbeat hosts remain online, and verify rate-limit responses are isolated by host identity rather than shared globally.
</verification>

<success_criteria>
- ENRL-04: Heartbeat freshness deterministically drives online/offline transitions.
- ENRL-05: Fleet UI displays status and last-seen from tenant-scoped host APIs.
- SECU-03: API and ingest routes enforce per-host traffic limits without collateral throttling for healthy hosts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-host-enrollment-and-trusted-connectivity/03-host-enrollment-and-trusted-connectivity-03-SUMMARY.md`
</output>
