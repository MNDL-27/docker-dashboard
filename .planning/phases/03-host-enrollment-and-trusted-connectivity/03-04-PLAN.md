---
phase: 03-host-enrollment-and-trusted-connectivity
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/middleware/rateLimit.ts
  - apps/api/src/index.ts
  - apps/web/src/app/(dashboard)/fleet/page.tsx
autonomous: true
gap_closure: true
requirements:
  - SECU-03
must_haves:
  truths:
    - "Per-host rate limits throttle abusive UI API traffic without globally blocking healthy users."
    - "Fleet host list artifact is wired into the active UI path."
  artifacts:
    - path: "apps/api/src/middleware/rateLimit.ts"
      provides: "UI API rate limiter factory"
      exports: ["createUiApiRateLimiters", "api"]
    - path: "apps/api/src/index.ts"
      provides: "UI API rate limiting applied to /api/* routes"
      mounts: ["/api/containers", "/api/audit", "/api/webhooks", "/api/alerts"]
    - path: "apps/web/src/app/(dashboard)/fleet/page.tsx"
      provides: "HostList component rendered in fleet UI"
      imports: ["HostList"]
  key_links:
    - from: "apps/api/src/middleware/rateLimit.ts"
      to: "apps/api/src/index.ts"
      via: "import and mount api limiter"
    - from: "apps/web/src/components/fleet/HostList.tsx"
      to: "apps/web/src/app/(dashboard)/fleet/page.tsx"
      via: "import and render with organizationId"
---

<objective>
Close 2 verification gaps from 03-VERIFICATION.md:
1. SECU-03 partial: Add UI API rate limiting for `/api/*` user-facing routes
2. Orphaned artifact: Wire `HostList.tsx` into fleet page to make it active in UI
</objective>

<context>
@.planning/phases/03-host-enrollment-and-trusted-connectivity/03-VERIFICATION.md
@.planning/phases/03-host-enrollment-and-trusted-connectivity/03-host-enrollment-and-trusted-connectivity-03-SUMMARY.md
@apps/api/src/middleware/rateLimit.ts
@apps/api/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UI API rate limiter to rateLimit.ts</name>
  <files>apps/api/src/middleware/rateLimit.ts</files>
  <action>
Add a new `createUiApiRateLimiters` function that exports rate limiters for user-facing UI API routes. The function should:
- Create a general API limiter using IP-based keying (similar to agent fallback)
- Use configurable limits via env vars: `UI_API_RATE_LIMIT_MAX` (default 120) and `UI_API_RATE_LIMIT_WINDOW_MS` (default 60000)
- Return an `api` RequestHandler that can be mounted on UI API routes

Export the new `createUiApiRateLimiters` function alongside the existing `createAgentRateLimiters`.
  </action>
  <verify>npm --prefix apps/api run build passes with new exports</verify>
  <done>Rate limiter factory for UI API exists and exports `api` handler</done>
</task>

<task type="auto">
  <name>Task 2: Wire UI API rate limiter in index.ts</name>
  <files>apps/api/src/index.ts</files>
  <action>
Import `createUiApiRateLimiters` from `./middleware/rateLimit` and instantiate the UI API rate limiter. Apply the `api` limiter to the `/api/containers`, `/api/audit`, `/api/webhooks`, and `/api/alerts` route mounts. Mount after session middleware but before the routes are used.

Pattern: 
```typescript
const uiApiRateLimiters = createUiApiRateLimiters();
app.use('/api/containers', uiApiRateLimiters.api, actionRoutes);
app.use('/api/audit', uiApiRateLimiters.api, auditRoutes);
app.use('/api/webhooks', uiApiRateLimiters.api, webhookRoutes);
app.use('/api/alerts', uiApiRateLimiters.api, alertRoutes);
```
  </action>
  <verify>npm --prefix apps/api run build passes; limiter applied to all /api/* routes</verify>
  <done>All UI API routes are now protected by rate limiting per SECU-03</done>
</task>

<task type="auto">
  <name>Task 3: Wire HostList component into fleet page</name>
  <files>apps/web/src/app/(dashboard)/fleet/page.tsx</files>
  <action>
Import `HostList` from `@/components/fleet/HostList` and render it in the fleet page. The HostList component takes `organizationId` as prop, which is already available in the fleet page as `orgId`.

Add the HostList after the project cards grid section. This provides an organization-wide view of all enrolled hosts with their connectivity status.

Note: The existing fleet page shows projects with host counts. Adding HostList gives users visibility into actual host instances with their online/offline status and last-seen timestamps.
  </action>
  <verify>npm --prefix apps/web run build passes; HostList is imported and rendered</verify>
  <done>HostList component is now active in the UI, rendering at /fleet</done>
</task>

</tasks>

<verification>
- [ ] `createUiApiRateLimiters` exports `api` handler in rateLimit.ts
- [ ] `/api/containers`, `/api/audit`, `/api/webhooks`, `/api/alerts` have rate limiter mounted
- [ ] HostList is imported in fleet/page.tsx and rendered with organizationId prop
- [ ] All builds pass (apps/api and apps/web)
</verification>

<success_criteria>
Both gaps from 03-VERIFICATION.md are closed:
1. SECU-03: UI API routes now have rate limiting coverage
2. HostList: Component is wired and active in fleet UI
</success_criteria>

<output>
After completion, create `.planning/phases/03-host-enrollment-and-trusted-connectivity/{phase}-{plan}-SUMMARY.md`
</output>
