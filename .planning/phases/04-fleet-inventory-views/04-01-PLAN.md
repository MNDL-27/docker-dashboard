---
phase: 04-fleet-inventory-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - packages/agent/docker/client.go
  - packages/agent/client/api.go
  - apps/api/src/routes/agent.ts
  - apps/api/src/services/container.ts
  - apps/api/src/routes/hosts.ts
  - apps/api/src/__tests__/fleet-inventory-routes.test.ts
autonomous: true
requirements: [INVT-01, INVT-02, INVT-03]
must_haves:
  truths:
    - "User can load fleet-level host and container totals for the selected org/project scope."
    - "User can open a host and receive container records with status, image, labels, and restart count."
    - "User search/filter inputs are enforced by tenant-scoped API queries, not client-only filtering."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Host/container snapshot fields required by Phase 4 cards"
      contains: "restartCount"
    - path: "apps/api/src/routes/hosts.ts"
      provides: "Fleet aggregate + host container query contract with filters/search"
      exports: ["hostsRouter"]
    - path: "apps/api/src/__tests__/fleet-inventory-routes.test.ts"
      provides: "Regression coverage for counts/search/filters and tenant scope"
      contains: "GET /hosts"
  key_links:
    - from: "packages/agent/docker/client.go"
      to: "apps/api/src/services/container.ts"
      via: "snapshot payload fields persisted during ingest sync"
      pattern: "restart|created|network|volume"
    - from: "apps/api/src/routes/hosts.ts"
      to: "apps/api/src/services/scopedAccess.ts"
      via: "scopedHostWhere/scopedContainerWhere in query composition"
      pattern: "scopedHostWhere|scopedContainerWhere"
---

<objective>
Establish a complete inventory data contract so fleet totals and host container cards can be powered by tenant-safe API responses.

Purpose: Deliver the backend and ingest foundations for INVT-01/02/03 before UI rewrite work begins.
Output: Expanded snapshot schema fields, richer host/container API query contract, and regression tests for scoped counts/search/filter behavior.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fleet-inventory-views/04-CONTEXT.md
@.planning/phases/04-fleet-inventory-views/04-RESEARCH.md
@apps/api/src/routes/hosts.ts
@apps/api/src/services/container.ts
@packages/agent/docker/client.go
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand inventory snapshot schema and ingest payload fields</name>
  <files>prisma/schema.prisma, packages/agent/docker/client.go, packages/agent/client/api.go, apps/api/src/routes/agent.ts, apps/api/src/services/container.ts</files>
  <action>Add host/container snapshot fields required by locked card decisions: host IP address, agent version, and CPU/memory summary; container restart count, created timestamp, networks, and volumes. Update agent payload collection and ingest parsing/upsert mapping so these values are persisted transactionally with existing sync behavior. Keep compatibility with existing snapshot writes and do not add metrics/log/action fields (deferred).</action>
  <verify>npm --prefix apps/api run build && go test ./... ./packages/agent/...</verify>
  <done>Inventory-required fields are available in persisted host/container data and ingest accepts the expanded snapshot payload without regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Extend host inventory routes for aggregate counts and scoped search/filter contract</name>
  <files>apps/api/src/routes/hosts.ts</files>
  <action>Extend `GET /hosts` to return fleet-level totals (host count and container count) alongside host cards for selected scope. Extend `GET /hosts/:id/containers` query contract for full search over name/image/labels and multi-select filters for status/project/host, composed inside `scopedHostWhere`/`scopedContainerWhere`. Ensure host filter values are validated against tenant scope, and keep all filtering server-side for correctness. Using scoped-access helpers per established decisions even if a simpler local filter path seems faster.</action>
  <verify>npm --prefix apps/api run build</verify>
  <done>Inventory APIs return scoped totals and filtered container results that satisfy INVT-01/02/03 payload expectations.</done>
</task>

<task type="auto">
  <name>Task 3: Add API regression tests for Phase 4 inventory contracts</name>
  <files>apps/api/src/__tests__/fleet-inventory-routes.test.ts</files>
  <action>Create a dedicated vitest/supertest suite covering: scoped fleet totals from `GET /hosts`; host container payload fields (status/image/labels/restartCount); full search across name/image/labels; multi-select status/project/host filtering; and cross-tenant denial for mismatched scope. Reuse existing auth/scope test setup patterns to keep tests deterministic.</action>
  <verify>npm --prefix apps/api run test -- fleet-inventory-routes.test.ts && npm --prefix apps/api run build</verify>
  <done>Automated tests prove the new inventory API contracts and scope boundaries work as specified.</done>
</task>

</tasks>

<verification>
Run the new inventory API test suite and confirm route payloads include fleet totals plus filter/search semantics while preserving tenant isolation behavior.
</verification>

<success_criteria>
- INVT-01: `GET /hosts` includes fleet-level host/container totals for selected scope.
- INVT-02: `GET /hosts/:id/containers` returns container records with status, image, labels, and restart count.
- INVT-03: Search and multi-select filters are enforced server-side and validated by regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-fleet-inventory-views/04-fleet-inventory-views-01-SUMMARY.md`
</output>
