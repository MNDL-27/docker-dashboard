---
phase: 04-fleet-inventory-views
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/web/src/app/(dashboard)/fleet/page.tsx
  - apps/web/src/components/fleet/FleetInventoryView.tsx
  - apps/web/src/components/fleet/HostCard.tsx
  - apps/web/src/components/fleet/ContainerCardGrid.tsx
  - apps/web/src/components/fleet/FleetFilters.tsx
  - apps/web/src/lib/api.ts
  - apps/web/src/app/(dashboard)/settings/members/page.tsx
autonomous: true
requirements: [INVT-01, INVT-02, INVT-03]
must_haves:
  truths:
    - "User sees a fleet overview made of host cards and can click a host card to expand container cards below it."
    - "User sees detailed host and container card content by default, with density options available in settings."
    - "User can open a contextual filter panel, stage search/filter edits, and apply them only when Apply is pressed."
  artifacts:
    - path: "apps/web/src/components/fleet/FleetInventoryView.tsx"
      provides: "Fleet drill-down layout, expansion behavior, and query wiring"
      min_lines: 180
    - path: "apps/web/src/components/fleet/FleetFilters.tsx"
      provides: "Contextual filter UI with draft/applied state and Apply control"
      contains: "Apply"
    - path: "apps/web/src/app/(dashboard)/settings/members/page.tsx"
      provides: "Inventory density preference controls (Simple/Standard/Detailed)"
      contains: "Detailed"
  key_links:
    - from: "apps/web/src/components/fleet/FleetInventoryView.tsx"
      to: "apps/api/src/routes/hosts.ts"
      via: "applied filter query parameters in API requests"
      pattern: "search|status|project|host"
    - from: "apps/web/src/app/(dashboard)/settings/members/page.tsx"
      to: "apps/web/src/components/fleet/HostCard.tsx"
      via: "persisted density preference read for card rendering mode"
      pattern: "localStorage|density"
---

<objective>
Implement the locked Fleet Inventory UX: card-based drill-down, detailed density by default, and contextual draft/apply filtering.

Purpose: Deliver the primary Phase 4 user experience on `/fleet` using the API contracts from Plan 01.
Output: Fleet card layout, inline host expansion, container card grid, contextual filter panel with Apply workflow, and persisted density preference controls in settings.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fleet-inventory-views/04-CONTEXT.md
@.planning/phases/04-fleet-inventory-views/04-RESEARCH.md
@.planning/phases/03-host-enrollment-and-trusted-connectivity/03-host-enrollment-and-trusted-connectivity-03-SUMMARY.md
@apps/web/src/app/(dashboard)/fleet/page.tsx
@apps/web/src/components/fleet/HostList.tsx
@apps/web/src/components/fleet/ContainerTable.tsx
@apps/web/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace fleet table flow with host-card drill-down and inline container card expansion</name>
  <files>apps/web/src/app/(dashboard)/fleet/page.tsx, apps/web/src/components/fleet/FleetInventoryView.tsx, apps/web/src/components/fleet/HostCard.tsx, apps/web/src/components/fleet/ContainerCardGrid.tsx</files>
  <action>Refactor `/fleet` to render a fleet overview of host cards (not table rows). On host-card click, expand the host's container card grid directly below that host in the same page context. Host cards must include locked detailed fields: name, status, container count, last seen, IP, agent version, CPU/memory summary. Container cards must include locked detailed fields: name, status, image, restarts, created, labels, ports, networks, volumes. Keep cards informational only; do not add container detail-page navigation, metrics, logs, or action buttons (deferred).</action>
  <verify>npm --prefix apps/web run lint</verify>
  <done>`/fleet` uses host cards with inline expand-to-container-grid drill-down and displays the required detailed fields without deferred feature leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Implement contextual filter panel with draft vs applied state and full search</name>
  <files>apps/web/src/components/fleet/FleetInventoryView.tsx, apps/web/src/components/fleet/FleetFilters.tsx, apps/web/src/lib/api.ts</files>
  <action>Add a contextual `Filter` button that opens filter controls only on demand. Implement separate `draftFilters` and `appliedFilters` state so search/multi-select changes do not alter displayed results until user clicks `Apply`. Support full search across name/image/labels and multi-select status/project/host values, and send only `appliedFilters` to API queries. Preserve tenant/org scope behavior from earlier phases and avoid client-only security filtering.</action>
  <verify>npm --prefix apps/web run lint</verify>
  <done>Users can stage search/filter edits in contextual UI and only committed Apply interactions change host/container results.</done>
</task>

<task type="auto">
  <name>Task 3: Add and persist inventory density setting in settings (default Detailed)</name>
  <files>apps/web/src/app/(dashboard)/settings/members/page.tsx, apps/web/src/components/fleet/FleetInventoryView.tsx, apps/web/src/components/fleet/HostCard.tsx, apps/web/src/components/fleet/ContainerCardGrid.tsx</files>
  <action>Add an "Inventory density" control in settings with `Simple`, `Standard`, and `Detailed` options, defaulting to `Detailed`. Persist the selection in localStorage (or existing client preference utility) and read it in fleet card components to switch rendered detail depth consistently for host and container cards. Maintain Detailed as default when no stored preference exists.</action>
  <verify>npm --prefix apps/web run lint</verify>
  <done>Density preference is configurable in settings, persists across sessions, and controls fleet card detail level with Detailed as default.</done>
</task>

</tasks>

<verification>
Run type/lint checks for web and confirm `/fleet` interaction supports host-card drill-down, contextual filters with Apply gating, and persisted density modes from settings.
</verification>

<success_criteria>
- INVT-01: Fleet overview visibly includes host/container totals in card-based fleet header/overview.
- INVT-02: Expanded host section renders container card grid with status/image/labels/restart count and other locked detailed fields.
- INVT-03: Search + multi-select filter workflow requires Apply and supports host/project/status scope combinations.
</success_criteria>

<output>
After completion, create `.planning/phases/04-fleet-inventory-views/04-fleet-inventory-views-02-SUMMARY.md`
</output>
