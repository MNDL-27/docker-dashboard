---
phase: 04-fleet-inventory-views
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - apps/web/src/components/fleet/FleetInventoryView.tsx
  - apps/web/src/components/fleet/ContainerCardGrid.tsx
  - apps/web/src/components/fleet/HostCard.tsx
  - apps/web/src/app/(dashboard)/fleet/page.tsx
  - apps/api/src/__tests__/fleet-inventory-routes.test.ts
autonomous: false
requirements: [INVT-02, INVT-03]
must_haves:
  truths:
    - "User sees explicit empty-state guidance when there are no hosts in scope."
    - "User sees a host-level no-containers state with a clear Docker-open action when a host has no containers."
    - "User sees a no-results message when search/filter criteria match nothing, without losing filter context."
  artifacts:
    - path: "apps/web/src/components/fleet/FleetInventoryView.tsx"
      provides: "No-host and no-results empty states with guided hints"
      contains: "No containers match your search"
    - path: "apps/web/src/components/fleet/ContainerCardGrid.tsx"
      provides: "Host-expanded no-container state with open-Docker call-to-action"
      contains: "Open Docker"
    - path: "apps/api/src/__tests__/fleet-inventory-routes.test.ts"
      provides: "Coverage for multi-filter combinations and search semantics"
      contains: "labels"
  key_links:
    - from: "apps/web/src/components/fleet/FleetInventoryView.tsx"
      to: "apps/web/src/components/fleet/FleetFilters.tsx"
      via: "applied filter context preserved through empty-state rendering"
      pattern: "appliedFilters"
    - from: "apps/web/src/components/fleet/FleetInventoryView.tsx"
      to: "apps/api/src/routes/hosts.ts"
      via: "search/filter params returning zero-result responses"
      pattern: "search"
---

<objective>
Polish Fleet Inventory usability with locked empty states and final verification so users can recover quickly from zero-data and zero-result paths.

Purpose: Complete the phase UX edge cases and validate the full drill-down/filter flow against user decisions.
Output: Guided empty states, no-results handling, strengthened filter-search regression checks, and human acceptance for the complete Phase 4 experience.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fleet-inventory-views/04-CONTEXT.md
@.planning/phases/04-fleet-inventory-views/04-RESEARCH.md
@.planning/phases/04-fleet-inventory-views/04-fleet-inventory-views-01-SUMMARY.md
@.planning/phases/04-fleet-inventory-views/04-fleet-inventory-views-02-SUMMARY.md
@apps/web/src/components/fleet/FleetInventoryView.tsx
@apps/web/src/components/fleet/ContainerCardGrid.tsx
@apps/api/src/__tests__/fleet-inventory-routes.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement locked empty states and guided hints for fleet and host inventory</name>
  <files>apps/web/src/components/fleet/FleetInventoryView.tsx, apps/web/src/components/fleet/ContainerCardGrid.tsx, apps/web/src/components/fleet/HostCard.tsx, apps/web/src/app/(dashboard)/fleet/page.tsx</files>
  <action>Add the required empty states: (a) no hosts in scope with quick-start host enrollment guidance, (b) no containers for expanded host with info message and button to open that host's Docker directly, and (c) no search results message exactly "No containers match your search". Include short guided mini-tutorial hints for each state, keep language concise, and avoid introducing deferred capabilities (container detail page, metrics, live logs, actions).</action>
  <verify>npm --prefix apps/web run lint</verify>
  <done>All locked empty states and guided hints render in the correct situations while keeping Phase 4 strictly informational.</done>
</task>

<task type="auto">
  <name>Task 2: Harden inventory filter/search regressions for combined multi-select scenarios</name>
  <files>apps/api/src/__tests__/fleet-inventory-routes.test.ts</files>
  <action>Extend the inventory API test suite with combination cases that use search plus multi-select status/project/host filters together, including zero-result cases that power the no-results UI state. Keep coverage tenant-scoped and verify labels/image/name search semantics stay intact for full-search behavior.</action>
  <verify>npm --prefix apps/api run test -- fleet-inventory-routes.test.ts && npm --prefix apps/api run build</verify>
  <done>Combined filter/search behavior is regression-tested, including deterministic zero-result responses used by UI empty states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify full fleet inventory interaction quality</name>
  <files>apps/web/src/app/(dashboard)/fleet/page.tsx, apps/web/src/components/fleet/FleetInventoryView.tsx, apps/web/src/components/fleet/FleetFilters.tsx, apps/web/src/app/(dashboard)/settings/members/page.tsx</files>
  <action>Run one consolidated user verification pass after automation completes to confirm locked interaction decisions and empty-state guidance are correct in-browser.</action>
  <verify>Follow the step-by-step verification checklist in <how-to-verify> and capture pass/fail notes for each step.</verify>
  <done>User confirms the full phase behavior as approved, or reports concrete mismatches for follow-up fixes.</done>
  <what-built>End-to-end fleet inventory flow with card drill-down, contextual draft/apply filtering, density persistence, and empty-state guidance.</what-built>
  <how-to-verify>1) Open `/fleet` and confirm host cards show required detailed fields and fleet totals. 2) Click a host card and confirm container card grid expands below that host. 3) Click `Filter`, stage search + multi-select values, confirm results stay unchanged until `Apply`, then confirm filtered results update. 4) Trigger empty states: no hosts, no containers on expanded host, and no-match search; confirm messages and hints are present (including "No containers match your search"). 5) Change density in settings to Simple/Standard/Detailed, refresh, and confirm selection persists and card detail depth changes accordingly.</how-to-verify>
  <resume-signal>Type "approved" to close Phase 4, or reply with specific mismatches to address.</resume-signal>
</task>

</tasks>

<verification>
Automated checks pass for API and web linting, then a single human verification confirms locked UX decisions and empty-state behaviors in-browser.
</verification>

<success_criteria>
- INVT-02: Host expansion reliably shows container cards with required core metadata and correct empty host state.
- INVT-03: Contextual filters + full search work with Apply gating and provide clear no-results guidance.
- Phase 4 UX honors locked decisions and excludes deferred features.
</success_criteria>

<output>
After completion, create `.planning/phases/04-fleet-inventory-views/04-fleet-inventory-views-03-SUMMARY.md`
</output>
