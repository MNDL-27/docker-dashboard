---
phase: 05-metrics-telemetry-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - apps/api/src/services/metrics.ts
  - apps/api/src/__tests__/metrics-ingest-retention.test.ts
autonomous: true
requirements: [METR-01, METR-03]
must_haves:
  truths:
    - "Live metric samples are persisted against the correct scoped container records."
    - "Metrics older than 24 hours are deleted and cannot be queried by default API lookbacks."
    - "Retention behavior is deterministic and proven by automated tests."
  artifacts:
    - path: "apps/api/src/services/metrics.ts"
      provides: "Docker ID -> container UUID mapping, buffered writes, and 24h retention cutoff helpers"
      contains: "saveMetricsBatch"
    - path: "prisma/schema.prisma"
      provides: "Metric index support for retention and time-window queries"
      contains: "model ContainerMetric"
    - path: "apps/api/src/__tests__/metrics-ingest-retention.test.ts"
      provides: "Regression coverage for identity mapping and retention boundaries"
      contains: "24 hours"
  key_links:
    - from: "apps/api/src/services/metrics.ts"
      to: "prisma/schema.prisma"
      via: "Container lookup by hostId + dockerId before containerMetric writes"
      pattern: "findMany\\(.*dockerId"
    - from: "apps/api/src/services/metrics.ts"
      to: "ContainerMetric.timestamp"
      via: "retention delete cutoff based on now-24h"
      pattern: "deleteMany\\(.*timestamp"
---

<objective>
Establish telemetry data correctness and bounded retention before adding stream/history consumer contracts.

Purpose: METR-01 and METR-03 fail if ingest identity mapping is wrong or retention cutoff is non-deterministic.
Output: Hardened metric ingest service, schema/index support, and API-level retention regression coverage.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-metrics-telemetry-core/05-CONTEXT.md
@.planning/phases/05-metrics-telemetry-core/05-RESEARCH.md
@prisma/schema.prisma
@apps/api/src/services/metrics.ts
@apps/api/src/websocket/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix metric ingest identity mapping from Docker IDs to container UUIDs</name>
  <files>apps/api/src/services/metrics.ts</files>
  <action>Update metric ingestion to resolve agent-reported `containerId` (Docker ID) into internal `container.id` by scoped `hostId` + `dockerId` lookup before `containerMetric.createMany`. Drop unmatched samples with explicit structured logging counters (hostId, unmatched count) instead of writing invalid IDs. Keep buffered batching behavior and do not change agent payload shape. This honors locked fleet-first streaming behavior while fixing the known mapping risk documented in research.</action>
  <verify>npm --prefix apps/api run build</verify>
  <done>Metric rows persist using valid internal container UUIDs; ingest no longer relies on Docker IDs matching DB primary keys.</done>
</task>

<task type="auto">
  <name>Task 2: Harden 24-hour retention cutoff and storage/query indexes</name>
  <files>prisma/schema.prisma, apps/api/src/services/metrics.ts</files>
  <action>Ensure retention enforcement is deterministic: centralize a 24h cutoff helper used by cleanup and shared query defaults, run cleanup on interval, and guarantee timestamp comparisons are UTC-safe. Add/adjust metric timestamp indexing needed for efficient retention deletes and 15m/1h/6h/24h lookbacks. Keep retention bounded to MVP target (at least 24h), and do not extend to logs/actions/alerts (deferred phases).</action>
  <verify>npm --prefix apps/api run build</verify>
  <done>Retention logic consistently removes records older than cutoff and leaves <=24h samples queryable with acceptable query performance.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for ingest mapping and retention boundary behavior</name>
  <files>apps/api/src/__tests__/metrics-ingest-retention.test.ts</files>
  <action>Create a dedicated vitest/supertest suite covering: (1) valid Docker IDs map to internal container UUID metrics rows, (2) unknown Docker IDs are ignored with no invalid writes, and (3) retention removes data older than 24h while preserving recent samples. Include an assertion that default history lookbacks cannot return >24h samples, aligning implementation with METR-03 and research pitfall guidance.</action>
  <verify>npm --prefix apps/api run test -- metrics-ingest-retention.test.ts && npm --prefix apps/api run build</verify>
  <done>Automated tests prove metric ingest identity correctness and enforce retention behavior at the 24h boundary.</done>
</task>

</tasks>

<verification>
Run the new ingest/retention suite and confirm all persisted metrics reference scoped container UUIDs while >24h samples are excluded by retention and default lookback behavior.
</verification>

<success_criteria>
- METR-01 foundation: live metric persistence uses valid container UUID relations after Docker ID mapping.
- METR-03: samples older than 24h are removed and not returned by default telemetry history paths.
- Regression tests catch ID mapping drift and retention boundary regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-metrics-telemetry-core/05-metrics-telemetry-core-01-SUMMARY.md`
</output>
