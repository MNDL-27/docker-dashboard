---
phase: 05-metrics-telemetry-core
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - apps/api/src/services/telemetryQuery.ts
  - apps/api/src/routes/metrics.ts
  - apps/api/src/websocket/server.ts
  - apps/api/src/index.ts
  - apps/api/src/__tests__/metrics-routes-and-stream.test.ts
autonomous: true
requirements: [METR-01, METR-02]
must_haves:
  truths:
    - "User can request telemetry history windows of 15m, 1h, 6h, or 24h and receive scoped trend data."
    - "User receives streaming telemetry frames that emphasize KPI aggregates and top contributors."
    - "Live controls can pause/resume client updates and apply speed presets without breaking ingest."
  artifacts:
    - path: "apps/api/src/routes/metrics.ts"
      provides: "Tenant-scoped telemetry history and live KPI endpoints with window preset validation"
      exports: ["metricsRouter"]
    - path: "apps/api/src/services/telemetryQuery.ts"
      provides: "Server-side bucketed history and aggregate/top contributor query helpers"
      contains: "15m|1h|6h|24h"
    - path: "apps/api/src/websocket/server.ts"
      provides: "Scope-aware metrics fan-out and live control message handling"
      contains: "metrics"
    - path: "apps/api/src/__tests__/metrics-routes-and-stream.test.ts"
      provides: "Contract tests for history windows, scoping, and streaming payload shape"
      contains: "topContributors"
  key_links:
    - from: "apps/api/src/routes/metrics.ts"
      to: "apps/api/src/services/telemetryQuery.ts"
      via: "validated window preset drives server-side bucket query"
      pattern: "window.*15m|1h|6h|24h"
    - from: "apps/api/src/websocket/server.ts"
      to: "apps/api/src/routes/metrics.ts"
      via: "shared telemetry frame contract for aggregate + contributors"
      pattern: "aggregate|topContributors"
---

<objective>
Provide tenant-safe telemetry query and stream contracts for KPI-first fleet observability with live controls.

Purpose: METR-01 and METR-02 require a consistent backend contract for both real-time frames and preset history windows.
Output: Metrics route/service APIs, scoped websocket fan-out with live control semantics, and regression tests.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-metrics-telemetry-core/05-CONTEXT.md
@.planning/phases/05-metrics-telemetry-core/05-RESEARCH.md
@apps/api/src/index.ts
@apps/api/src/routes/hosts.ts
@apps/api/src/websocket/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scoped telemetry history API with locked window presets</name>
  <files>apps/api/src/services/telemetryQuery.ts, apps/api/src/routes/metrics.ts, apps/api/src/index.ts</files>
  <action>Create telemetry endpoints under `/api/metrics` that validate and accept only `15m|1h|6h|24h` history windows, org/project/host/container scope, and optional Top N inputs. Implement server-side bucketing (PostgreSQL time-bucket query via Prisma raw SQL or equivalent) returning KPI-ready trend points and restart indicators. Enforce tenant scope through existing scoped-access helpers; no unscoped fallback paths. Mount router in API index and keep response shape optimized for KPI-card consumers (aggregate + top contributors), not chart-first payloads.</action>
  <verify>npm --prefix apps/api run build</verify>
  <done>`/api/metrics` returns scoped, bucketed telemetry history for the four required windows with aggregate and contributor data suitable for fleet KPI cards.</done>
</task>

<task type="auto">
  <name>Task 2: Implement scope-aware telemetry stream fan-out with pause/resume and speed controls</name>
  <files>apps/api/src/websocket/server.ts, apps/api/src/services/telemetryQuery.ts</files>
  <action>Update websocket client handling so telemetry streaming is scope-aware (organization/project and selected host/container context), emits KPI-frame payloads (aggregate + topContributors), and supports control messages for pause/resume and speed presets. Keep ingest cadence unchanged per research recommendation; apply speed control to server fan-out/render cadence only. Preserve existing ws transport and avoid adding SSE or chart stream channels, honoring locked streaming and KPI-only decisions.</action>
  <verify>npm --prefix apps/api run build</verify>
  <done>Web clients can receive scoped telemetry frames and control live stream state (pause/resume/speed) without cross-tenant leakage or ingest regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Add API/websocket contract tests for windows, scoping, and telemetry frame shape</name>
  <files>apps/api/src/__tests__/metrics-routes-and-stream.test.ts</files>
  <action>Add deterministic tests covering: valid/invalid window preset handling; scoped access denial for out-of-scope org/project data; 24h-cap lookback behavior; and telemetry stream frame schema including `aggregate`, `topContributors`, and restart indicator fields. Include assertions that pause/resume/speed control messages update stream behavior and that deferred features (logs/actions/alerts) are absent from this contract.</action>
  <verify>npm --prefix apps/api run test -- metrics-routes-and-stream.test.ts && npm --prefix apps/api run build</verify>
  <done>Regression tests prove METR-01/02 backend contracts, stream controls, and tenant-safe telemetry fan-out semantics.</done>
</task>

</tasks>

<verification>
Run the telemetry API/stream suite and verify all four history windows, scoped live frame delivery, and pause/resume plus speed control behavior using the same contract consumed by the fleet UI.
</verification>

<success_criteria>
- METR-02: `/api/metrics` supports 15m/1h/6h/24h server-bucketed history windows.
- METR-01: websocket telemetry frames deliver aggregate KPIs plus top contributors in scope-aware real time.
- Live controls (pause/resume + speed presets) are handled without changing ingest behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/05-metrics-telemetry-core/05-metrics-telemetry-core-02-SUMMARY.md`
</output>
