---
phase: 05-metrics-telemetry-core
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - apps/web/src/lib/api.ts
  - apps/web/src/components/telemetry/TelemetryControls.tsx
  - apps/web/src/components/telemetry/TopContributorsList.tsx
  - apps/web/src/components/telemetry/TelemetryKpiPanel.tsx
  - apps/web/src/components/fleet/FleetInventoryView.tsx
autonomous: true
requirements: [METR-01, METR-02]
must_haves:
  truths:
    - "User can open fleet view and immediately see live KPI cards for CPU, memory, network, and restarts."
    - "User can pause/resume live telemetry and change speed presets while keeping current inventory context."
    - "User can switch history windows (15m/1h/6h/24h) and see trends update without leaving fleet telemetry."
    - "When multiple containers are in scope, user sees aggregate KPIs and a top contributor list."
    - "Telemetry focus auto-follows selected host/container context from fleet inventory interactions."
  artifacts:
    - path: "apps/web/src/components/telemetry/TelemetryKpiPanel.tsx"
      provides: "Fleet-level telemetry KPI card surface with balanced value/state emphasis"
      contains: "CPU"
    - path: "apps/web/src/components/telemetry/TelemetryControls.tsx"
      provides: "Pause/resume, speed presets, and window chips"
      contains: "15m"
    - path: "apps/web/src/components/telemetry/TopContributorsList.tsx"
      provides: "Top N active container ranking for current scope"
      contains: "top contributors"
    - path: "apps/web/src/components/fleet/FleetInventoryView.tsx"
      provides: "Inventory-context auto-follow wiring into telemetry scope selection"
      contains: "expandedHostId"
  key_links:
    - from: "apps/web/src/components/fleet/FleetInventoryView.tsx"
      to: "apps/web/src/components/telemetry/TelemetryKpiPanel.tsx"
      via: "selected host/container context passed as telemetry scope"
      pattern: "selected|scope|host|container"
    - from: "apps/web/src/components/telemetry/TelemetryKpiPanel.tsx"
      to: "apps/web/src/lib/api.ts"
      via: "history fetch + websocket live stream controls"
      pattern: "fetch|WebSocket|pause|speed"
---

<objective>
Deliver the fleet-level KPI telemetry UX using streaming updates, history window switching, and inventory-context auto-follow.

Purpose: Complete METR-01/02 user-facing behavior with locked decisions (KPI-only cards, live controls, Top N focus, aggregate + contributors).
Output: Reusable telemetry UI components, API client wiring, and fleet page integration.
</objective>

<execution_context>
@C:/Users/proti/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/proti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-metrics-telemetry-core/05-CONTEXT.md
@.planning/phases/05-metrics-telemetry-core/05-RESEARCH.md
@apps/web/src/components/fleet/FleetInventoryView.tsx
@apps/web/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add telemetry client contracts for history windows, streaming frames, and live controls</name>
  <files>apps/web/src/lib/api.ts</files>
  <action>Extend web API utilities with typed telemetry contracts for: history window fetch (`15m|1h|6h|24h`), live frame parsing (`aggregate`, `topContributors`, restart indicators), and websocket control messages (pause/resume, speed presets). Default Top N active focus to 5 (per research recommendation) unless server response overrides. Keep all contracts aligned to the Phase 5 backend payload and avoid introducing logs/action/alert client flows (deferred phases).</action>
  <verify>npm --prefix apps/web run build</verify>
  <done>Web telemetry client can fetch required windows and drive live controls using typed contracts with no `any`-based parsing.</done>
</task>

<task type="auto">
  <name>Task 2: Build KPI-first telemetry components with balanced value/state emphasis</name>
  <files>apps/web/src/components/telemetry/TelemetryControls.tsx, apps/web/src/components/telemetry/TopContributorsList.tsx, apps/web/src/components/telemetry/TelemetryKpiPanel.tsx</files>
  <action>Create telemetry UI components that render KPI cards only for CPU, memory, network, and restart indicators, plus explicit state badges (`Live`, `Paused`, `Stale`, `No data`) so value/state are equally legible. Implement controls for pause/resume, speed presets, and history window switching. Keep trend context compact inside cards (sparkline/mini-trend), not chart-first layouts, to honor locked decisions.</action>
  <verify>npm --prefix apps/web run lint && npm --prefix apps/web run build</verify>
  <done>Fleet telemetry UI exposes all locked controls and indicators via KPI cards with clear freshness/state semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate telemetry panel into fleet inventory with auto-follow scope</name>
  <files>apps/web/src/components/fleet/FleetInventoryView.tsx, apps/web/src/components/telemetry/TelemetryKpiPanel.tsx</files>
  <action>Embed the telemetry panel at fleet level and wire it to inventory context so selection auto-follows currently expanded host and selected container when present. Ensure multi-container scope shows aggregate cards + top contributor list, and default fallback remains Top N active containers when no explicit selection exists. Keep existing inventory card drill-down behavior intact and avoid reworking deferred container lifecycle/logging/alert surfaces.</action>
  <verify>npm --prefix apps/web run lint && npm --prefix apps/web run build</verify>
  <done>Telemetry panel is available from fleet entry, follows inventory context automatically, and supports aggregate + contributors for multi-container scope.</done>
</task>

</tasks>

<verification>
Run web lint/build and confirm fleet view shows KPI-only live telemetry with working pause/resume, speed presets, and 15m/1h/6h/24h switches while context follows host/container selections.
</verification>

<success_criteria>
- METR-01: fleet entry shows streaming KPI cards for CPU/memory/network/restart with control over live flow.
- METR-02: users can switch all required history windows and see trend updates in telemetry cards.
- Locked UX decisions are fully implemented; deferred logs/actions/alerts remain excluded.
</success_criteria>

<output>
After completion, create `.planning/phases/05-metrics-telemetry-core/05-metrics-telemetry-core-03-SUMMARY.md`
</output>
